<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48">
  <!-- Mirrors the Logo.tsx placeholder pill: primary-600 circle, "AL" in
       the display font (falls back to system sans — favicons can't load
       webfonts). -->
  <circle cx="24" cy="24" r="24" fill="#186b68" />
  <text
    x="24"
    y="24"
    dy=".36em"
    text-anchor="middle"
    font-family="'Space Grotesk', system-ui, sans-serif"
    font-size="19"
    font-weight="600"
    fill="#ffffff"
  >
    AL
  </text>
</svg>
